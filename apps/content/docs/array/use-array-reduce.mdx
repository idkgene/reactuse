---
title: useArrayReduce
description: Collection of useful React hooks and utilities.
---

A hook providing a memoized version of the `Array.reduce` method for React, applying a reducer function to each element of the array, resulting in a single output value.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <ArrayReduceDemo />
  </Tab>
  <Tab value="Code">
  ```tsx
'use client';

import { useState } from 'react';

import { useArrayReduce } from '../useArrayReduce';

export default function ArrayReduceDemo() {
  const [inputValue, setInputValue] = useState('');
  const [reducerFunction, setReducerFunction] = useState(
    '(acc, item) => acc + item'
  );
  const [initialValue, setInitialValue] = useState('0');
  const items = inputValue.split(',').map(item => parseInt(item.trim(), 10));
  const reducedValue = useArrayReduce(
    items,
    (acc, item) => {
      try {
        const func = eval(reducerFunction);
        return func(acc, item);
      } catch (error) {
        return acc;
      }
    },
    parseInt(initialValue, 10)
  );

return (

<div className="min-h-screen bg-gray-100 py-6 flex flex-col justify-center sm:py-12">
  <div className="relative py-3 sm:max-w-xl sm:mx-auto">
    <div className="absolute inset-0 bg-gradient-to-r from-cyan-400 to-light-blue-500 shadow-lg transform -skew-y-6 sm:skew-y-0 sm:-rotate-6 sm:rounded-3xl"></div>
    <div className="relative px-4 py-10 bg-white shadow-lg sm:rounded-3xl sm:p-20">
      <div className="mb-4">
        <label htmlFor="input" className="block text-gray-700 font-bold mb-2">
          Enter numbers (comma-separated):
        </label>
        <input
          id="input"
          type="text"
          value={inputValue}
          onChange={(e) => setInputValue(e.target.value)}
          className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
        />
      </div>
      <div className="mb-4">
        <label
          htmlFor="reducerFunction"
          className="block text-gray-700 font-bold mb-2"
        >
          Enter reducer function:
        </label>
        <input
          id="reducerFunction"
          type="text"
          value={reducerFunction}
          onChange={(e) => setReducerFunction(e.target.value)}
          className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
        />
      </div>
      <div className="mb-6">
        <label
          htmlFor="initialValue"
          className="block text-gray-700 font-bold mb-2"
        >
          Enter initial value:
        </label>
        <input
          id="initialValue"
          type="text"
          value={initialValue}
          onChange={(e) => setInitialValue(e.target.value)}
          className="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
        />
      </div>
      <div className="bg-gray-100 p-4 rounded-md">
        <p className="text-lg font-semibold mb-2">Reduced value:</p>
        <p className="text-gray-800">{reducedValue}</p>
      </div>
    </div>
  </div>
</div>
); }

````
</Tab>
</Tabs>

## Installation

```bash
npx changeelog/reactuse@latest add useArrayReduce
````

## Usage

```tsx
import { useArrayReduce } from '#';

import { useState } from 'react';

const Component = () => {
  const [numbers, setNumbers] = useState([1, 2, 3, 4]);

  const sum = useArrayReduce(
    numbers,
    (accumulator, number) => accumulator + number,
    0,
  );

  return (
    <div>
      <p>Sum: {sum}</p>
    </div>
  );
};

export default Component;
```

```tsx
import { useArrayReduce } from '#';

import { useState } from 'react';

const Component = () => {
  const [objects, setObjects] = useState([{ count: 1 }, { count: 2 }]);

  const totalCount = useArrayReduce(
    objects,
    (accumulator, obj) => accumulator + obj.count,
    0,
  );

  return (
    <div>
      <p>Total Count: {totalCount}</p>
    </div>
  );
};

export default Component;
```

## Reference

### useArrayReduce

`useArrayReduce` is a React hook that provides a memoized implementation of the `Array.prototype.reduce` method.

<TypeTable
  type={{
    list: {
      description:
        'The array to reduce. If list is not an array, an error message is logged to the console and the hook returns the initial value.',
      type: 'T[]',
    },
    reducer: {
      description: 'A "reducer" function that accumulates the array elements.',
      type: 'UseArrayReduceCallback<U, T, U>',
    },
    initialValue: {
      description:
        'A value to initialize the accumulator the first time the callback is called.',
      type: 'U',
    },
  }}
/>

### UseArrayReduceCallback

Represents a reducer function used in the `useArrayReduce` function.

<TypeTable
  type={{
    previousValue: {
      description:
        'The value previously returned in the last invocation of the reducer.',
      type: 'PV',
    },
    currentValue: {
      description: 'The current element being processed.',
      type: 'CV',
    },
    currentIndex: {
      description: 'The index of the current element.',
      type: 'number',
    },
    array: {
      description: 'The array being processed.',
      type: 'CV[]',
    },
  }}
/>

### Return Value

<TypeTable
  type={{
    U: {
      description:
        'The value that results from running the "reducer" callback function to completion over the entire array.',
    },
  }}
/>

import ArrayReduceDemo from '../../../hooks/Array/useArrayReduce/useArrayReduce.demo';
