---
title: useList
description: Collection of useful React hooks and utilities.
---

The `useList` hook provides a flexible way to manage an array (list) within your React components. It returns the current list and a set of convenient functions for common list operations, such as adding, removing, updating, and clearing elements.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <UseListDemo />
  </Tab>
  <Tab value="Code">
  ```tsx
'use client';

import { useState } from 'react';

import { useList } from './useList';

export default function UseListDemo() {
  const [list, { push, removeAt, insertAt, updateAt, clear }] = useList<number>(
    [1, 2, 3]
  );
  const [inputValue, setInputValue] = useState('');

const handleAddItem = () => {
if (inputValue.trim() !== '') {
push(Number(inputValue));
setInputValue('');
}
};

return (

<div className="min-h-screen bg-gray-100 flex items-center justify-center">
  <div className="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
    <div className="mb-4">
      <label htmlFor="inputItem" className="block text-gray-700 font-bold mb-2">
        Add Item:
      </label>
      <div className="flex">
        <input
          type="text"
          id="inputItem"
          className="flex-1 px-4 py-2 border border-gray-300 rounded-l-md focus:outline-none focus:ring-2 focus:ring-blue-500"
          value={inputValue}
          onChange={(e) => setInputValue(e.target.value)}
        />
        <button
          className="bg-blue-500 hover:bg-blue-600 text-white font-bold px-4 py-2 rounded-r-md"
          onClick={handleAddItem}
        >
          Add
        </button>
      </div>
    </div>
    <ul className="space-y-2">
      {list.map((item, index) => (
        <li key={index} className="flex items-center justify-between">
          <span>{item}</span>
          <div>
            <button
              className="text-blue-500 hover:text-blue-600 font-bold px-2 py-1"
              onClick={() => removeAt(index)}
            >
              Remove
            </button>
            <button
              className="text-blue-500 hover:text-blue-600 font-bold px-2 py-1"
              onClick={() => updateAt(index, Number(`${item}`))}
            >
              Update
            </button>
          </div>
        </li>
      ))}
    </ul>
    <div className="mt-4">
      <button
        className="bg-red-500 hover:bg-red-600 text-white font-bold px-4 py-2 rounded"
        onClick={clear}
      >
        Clear List
      </button>
    </div>
  </div>
</div>
); }

````
</Tab>
</Tabs>

## Installation

```bash
npx changeelog/reactuse@latest add useList
````

## Usage

```tsx
import { useList } from '#';

const Component = () => {
  const [list, { set, push, removeAt, insertAt, updateAt, clear }] = useList([
    1, 2, 3,
  ]);

  return (
    <div>
      <ul>
        {list.map((item, index) => (
          <li key={index}>{item}</li>
        ))}
      </ul>
      <button onClick={() => push(4)}>Push</button>
      <button onClick={() => removeAt(1)}>Remove at index 1</button>
      <button onClick={() => insertAt(1, 5)}>Insert 5 at index 1</button>
      <button onClick={() => updateAt(0, 6)}>Update index 0 to 6</button>
      <button onClick={() => set([7, 8, 9])}>Set new list</button>
      <button onClick={clear}>Clear</button>
    </div>
  );
};

export default Component;
```

## Reference

### useList

`useList` is a hook that provides a list and utility functions for managing it.

<TypeTable
  type={{
    defaultList: {
      description: 'The initial list of elements.',
      type: 'T[]',
      default: '[]',
    },
  }}
/>

### UseListActions

`UseListActions` is an interface that defines utility functions for managing a list.

<TypeTable
  type={{
    set: {
      description: 'Sets a new list, replacing the existing one.',
      type: '(l: T[]) => void',
    },
    push: {
      description: 'Adds a new element to the end of the list.',
      type: '(element: T) => void',
    },
    removeAt: {
      description: 'Removes the element at the specified index.',
      type: '(index: number) => void',
    },
    insertAt: {
      description: 'Inserts an element at the specified index.',
      type: '(index: number, element: T) => void',
    },
    updateAt: {
      description: 'Updates the element at the specified index.',
      type: '(index: number, element: T) => void',
    },
    clear: {
      description: 'Clears the list by setting it to an empty array.',
      type: '() => void',
    },
  }}
/>

### Return Value

<TypeTable
  type={{
    '[T[], UseListActions]': {
      description: 'An array containing:',
      type: 'tuple',
      children: [
        {
          description: 'The current state of the list.',
          type: 'T[]',
        },
        {
          description:
            'An object containing utility functions to manage the list.',
          type: 'UseListActions',
        },
      ],
    },
  }}
/>

import UseListDemo from '../../../hooks/Array/useList/useList.demo';
