---
title: useArrayFind
description: Collection of useful React hooks and utilities.
---

A hook providing a memoized version of the `Array.find` method for React, finding the first element that passes the specified predicate function.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <ArrayFindDemo />
  </Tab>
  <Tab value="Code">
  ```tsx
'use client';

import { useState } from 'react';

import { useArrayFind } from './useArrayFind';

export default function ArrayFindDemo() {
  const [users, setUsers] = useState([
    { id: 1, name: 'Alice' },
    { id: 2, name: 'Bob' },
    { id: 3, name: 'Charlie' },
  ]);
  const [searchId, setSearchId] = useState('');

const findUserById = (user: { id: number }) => user.id === parseInt(searchId);
const foundUser = useArrayFind(users, findUserById);

return (

<div className="min-h-screen bg-gray-100 py-6 flex flex-col justify-center sm:py-12">
  <div className="relative py-3 sm:max-w-xl sm:mx-auto">
    <div className="absolute inset-0 bg-gradient-to-r from-cyan-400 to-light-blue-500 shadow-lg transform -skew-y-6 sm:skew-y-0 sm:-rotate-6 sm:rounded-3xl"></div>
    <div className="relative px-4 py-10 bg-white shadow-lg sm:rounded-3xl sm:p-20">
      <div className="mb-6">
        <label htmlFor="users" className="block text-gray-700 font-bold mb-2">
          Users:
        </label>
        <textarea
          id="users"
          value={JSON.stringify(users, null, 2)}
          onChange={(e) => setUsers(JSON.parse(e.target.value))}
          className="w-full px-3 py-2 text-gray-700 border rounded-md focus:outline-none focus:ring-2 focus:ring-cyan-600"
          rows={6}
        />
      </div>
      <div className="mb-6">
        <label
          htmlFor="searchId"
          className="block text-gray-700 font-bold mb-2"
        >
          Search User ID:
        </label>
        <input
          type="text"
          id="searchId"
          value={searchId}
          onChange={(e) => setSearchId(e.target.value)}
          className="w-full px-3 py-2 text-gray-700 border rounded-md focus:outline-none focus:ring-2 focus:ring-cyan-600"
        />
      </div>
      <div>
        <p className="text-gray-700 font-bold mb-2">Found User:</p>
        {foundUser ? (
          <pre className="text-gray-600 bg-gray-100 p-4 rounded-md">
            {JSON.stringify(foundUser, null, 2)}
          </pre>
        ) : (
          <p className="text-gray-600">No user found.</p>
        )}
      </div>
    </div>
  </div>
</div>
); }

````
</Tab>
</Tabs>

## Installation

```bash
npx changeelog/reactuse@latest add useArrayFind
````

## Usage

```tsx
import { useArrayFind } from '#';

import { useState } from 'react';

const Component = () => {
  const [users, setUsers] = useState([
    { id: 1, name: 'Alice' },
    { id: 2, name: 'Bob' },
    { id: 3, name: 'Charlie' },
  ]);

  const findUserById = (user: { id: number }) => user.id === 2;

  const user = useArrayFind(users, findUserById);

  return (
    <div>
      <p>User: {user ? user.name : 'None'}</p>
    </div>
  );
};

export default Component;
```

```tsx
import { useArrayFind } from '#';

import { useState } from 'react';

const Component = () => {
  const [books, setBooks] = useState([
    { title: 'Book A', author: 'Author A' },
    { title: 'Book B', author: 'Author B' },
    { title: 'Book C', author: 'Author A' },
  ]);

  const findBookByTitle = (book: { title: string }) => book.title === 'Book B';

  const book = useArrayFind(books, findBookByTitle);

  return (
    <div>
      <p>Book: {book ? book.title : 'None'}</p>
    </div>
  );
};

export default Component;
```

## Reference

### useArrayFind

`useArrayFind` is a React hook that provides a memoized implementation of the `Array.prototype.find` method.

<TypeTable
  type={{
    list: {
      description: 'The array to search.',
      type: 'T[]',
    },
    predicate: {
      description: 'A function to test each element in the array.',
      type: 'UseArrayFindPredicate',
    },
  }}
/>

### UseArrayFindPredicate

Represents a predicate function used in the `useArrayFind` function.

<TypeTable
  type={{
    element: {
      description: 'The current element being processed.',
      type: 'T',
    },
    index: {
      description: 'The index of the current element.',
      type: 'number',
    },
    array: {
      description: 'The array being processed.',
      type: 'T[]',
    },
  }}
/>

### Return Value

<TypeTable
  type={{
    'T | undefined': {
      description:
        'The first element in the array that satisfies the provided testing function. Otherwise, undefined is returned.',
    },
  }}
/>

import ArrayFindDemo from '../../../hooks/Array/useArrayFind/useArrayFind.demo';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

;
