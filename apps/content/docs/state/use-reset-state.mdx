---
title: useResetState
description: Collection of useful React hooks and utilities.
---

`useResetState` is a custom hook that manages state with a reset function to revert to the initial state.

## Usage

```tsx
import { useResetState } from '#';
import * as React from 'react';

const Component = () => {
  const [count, setCount, resetCount] = useResetState(0);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={() => setCount(count + 1)}>Increment</button>
      <button onClick={resetCount}>Reset</button>
    </div>
  );
};

export default Component;
```

## Type Declarations

```ts
export declare const useResetState: <T>(
  initialState: T | (() => T)
) => [T, Dispatch<SetStateAction<T>>, ResetState];

type ResetState = () => void;
```

## Reference

#### useResetState

| Property     | Type             | Description                                                           |
| :----------- | :--------------- | :-------------------------------------------------------------------- |
| initialState | `T \| (() => T)` | The initial state value or a function that returns the initial state. |

#### Returns

| Property                                       | Type                                                                                                      | Description |
| :--------------------------------------------- | :-------------------------------------------------------------------------------------------------------- | :---------- |
| `[T, Dispatch<SetStateAction<T>>, ResetState]` | An array containing: the current state value, a function to update the state value, and a reset function. |
