---
title: useTimestamp
description: Current timestamp.
---

## Installation

```bash
npx changeelog/reactuse@latest add useTimestamp
```

## Usage

```tsx
import { useTimestamp } from '#';

const Component = () => {
  // Basic usage
  const timestamp = useTimestamp();

  // With controls
  const {
    timestamp: controlledTimestamp,
    pause,
    resume,
  } = useTimestamp({ controls: true });

  return (
    <div>
      <p>Timestamp: {timestamp}</p>
      <p>Controlled Timestamp: {controlledTimestamp}</p>
      <button onClick={pause}>Pause</button>
      <button onClick={resume}>Resume</button>
    </div>
  );
};
```

## Reference

### useTimestamp

The `useTimestamp` hook provides the current timestamp and optionally exposes controls for pausing and resuming updates.

<TypeTable
  type={{
    options: {
      description: '(Optional) Options for the useTimestamp hook.',
      type: 'UseTimestampOptions',
      default: '{}',
    },
  }}
  generic={{
    Controls: {
      description: 'Whether controls are enabled.',
      default: 'false',
    },
  }}
/>

### UseTimestampOptions

<TypeTable
  type={{
    controls: {
      description: 'Expose pause and resume controls.',
      type: 'boolean',
      default: 'false',
    },
    offset: {
      description: 'Offset value added to the timestamp.',
      type: 'number',
      default: '0',
    },
    immediate: {
      description: 'Update the timestamp immediately on mount.',
      type: 'boolean',
      default: 'true',
    },
    interval: {
      description:
        'Update interval in milliseconds or use "requestAnimationFrame" for smoother updates.',
      type: '"requestAnimationFrame" | number',
      default: '"requestAnimationFrame"',
    },
    callback: {
      description:
        'Callback function invoked on each update with the current timestamp.',
      type: '(timestamp: number) => void',
    },
  }}
/>

### Return Value

If `controls` is `false`, the hook returns the current `timestamp` as a `number`.

If `controls` is `true`, the hook returna an object containing the following properties:

<TypeTable
  type={{
    timestamp: {
      description: 'The current timestamp.',
      type: 'number',
    },
    pause: {
      description: 'Function to pause the timestamp updates.',
      type: '() => void',
    },
    resume: {
      description: 'Function to resume the timestamp updates.',
      type: '() => void',
    },
  }}
/>
