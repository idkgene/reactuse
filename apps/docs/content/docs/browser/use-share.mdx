---
title: useShare
description: Functionality for sharing content using the Web Share API
---

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <ShareDemo />
  </Tab>
  <Tab value="Code">```tsx // demo code ```</Tab>
</Tabs>

## Installation

```bash
npx changeelog/reactuse@latest add useShare
```

## Usage

```tsx
import { useShare } from '#';
import * as React from 'react';

const Component = () => {
  const { isSupported, share } = useShare();

  const handleShare = () => {
    if (isSupported) {
      share({
        title: 'Check out this cool website!',
        text: 'I found this amazing content.',
        url: 'https://example.com',
      });
    } else {
      console.log('Web Share API is not supported in this browser.');
    }
  };

  return (
    <div>
      <button onClick={handleShare} disabled={!isSupported}>
        Share
      </button>
    </div>
  );
};

export default Component;
```

## Reference

### useShare

`useShare` is a hook that provides functionality for sharing content using the Web Share API.

<TypeTable
  type={{
    initialOptions: {
      description: 'Initial options for sharing.',
      type: 'UseShareOptions',
      default: 'undefined',
    },
  }}
/>

### UseShareOptions

An interface defining the options for sharing content:

<TypeTable
  type={{
    title: {
      description: 'The title of the shared content.',
      type: 'string',
      default: 'undefined',
    },
    text: {
      description: 'The text to share.',
      type: 'string',
      default: 'undefined',
    },
    url: {
      description: 'The URL to share.',
      type: 'string',
      default: 'undefined',
    },
    files: {
      description: 'An array of files to share.',
      type: 'File[]',
      default: '[]',
    },
  }}
/>

### Return Value

Returns an object with the following properties:

<TypeTable
  type={{
    isSupported: {
      description:
        'Indicates whether the Web Share API is supported by the browser.',
      type: 'boolean',
    },
    share: {
      description: 'A function to initiate the share action.',
      type: '(overrideOptions?: UseShareOptions) => Promise',
    },
  }}
/>

import ShareDemo from '../../../hooks/Browser/useShare/useShare.demo.tsx';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { File, Folder, Files } from 'fumadocs-ui/components/files';
