---
title: useBoolean
description: Manage a boolean state value with a toggle function
---

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <UseBooleanDemo />
  </Tab>
  ```tsx tab="Code"
  export default function UseBooleanDemo() {
  const [isToggled, toggleIsToggled, setIsToggled] = useBoolean(false);

return (

<>
  <p>
    The toggle is{' '}
    {isToggled ? (
      <span className="text-[#44bd87]">ON</span>
    ) : (
      <span className="text-[#ff0f0f]">OFF</span>
    )}
    .
  </p>
  <div>
    <Button onClick={toggleIsToggled}>Toggle</Button>
    <Button onClick={() => setIsToggled(true)}>Turn ON</Button>
    <Button onClick={() => setIsToggled(false)}>Turn OFF</Button>
  </div>
</>
 ); 
}
```
</Tabs>

## Usage

```tsx
import { useBoolean } from '#';
import * as React from 'react';

const Component = () => {
const [isVisible, toggleVisibility, setVisibility] = useBoolean(false);

return (
  <div>
    <p>Visibility: {isVisible ? 'Visible' : 'Hidden'}</p>
    <button onClick={toggleVisibility}>Toggle Visibility</button>
    <button onClick={() => setVisibility(true)}>Show</button>
    <button onClick={() => setVisibility(false)}>Hide</button>
  </div>
);
};

export default Component;
```

## Installation

<Tabs items={['CLI', 'Manual']}>
```bash tab="CLI"
npx changeelog/reactuse@latest add createGlobalState
```

  <Tab value="Manual">
<Steps>
<Step>
**If not already created, create a folder for hooks in your project. Your structure could look like this:**

<Files>
  <Folder name="app" defaultOpen>
    <File name="layout.tsx" />
    <File name="page.tsx" />
    <File name="global.css" />
  </Folder>
  <Folder name="components">
    <File name="button.tsx" />
    <File name="tabs.tsx" />
    <File name="dialog.tsx" />
  </Folder>
    <Folder name="hooks">
    <File name="hook.ts" />
    <File name="custom-hook.ts" />
  </Folder>
  <File name="package.json" />
</Files>
 </Step>
<Step>
**Copy and paste the following code into your project.**

```ts tab="Manual"
import { useState, useCallback } from 'react';

import type { SetStateAction, Dispatch } from 'react';

export type UseBooleanReturn = [
  boolean,
  () => void,
  Dispatch<SetStateAction<boolean>>,
];

export const useBoolean = (initialValue: boolean = false): UseBooleanReturn => {
  const [value, setValue] = useState(initialValue);

  const toggle = useCallback(() => {
    setValue((prevValue) => !prevValue);
  }, []);

  return [value, toggle, setValue];
};
```

</Step>
<Step>
**Update the import paths to match your project setup if needed.**
</Step>
</Steps></Tab>
  </Tabs>

## Reference

### useBoolean

`useBoolean` is a hook that manages a boolean state value with a toggle function.

<TypeTable
  type={{
    initialValue: {
      description: 'The initial boolean value.',
      type: 'boolean',
      default: 'false',
    },
  }}
/>

### Return Value

Returns an array with the following elements:

<TypeTable
  type={{
    value: {
      description: 'The current boolean value.',
      type: 'boolean',
    },
    toggle: {
      description: 'Function to toggle the boolean value.',
      type: '() => void',
    },
    setValue: {
      description: 'Function to set the boolean value directly.',
      type: 'Dispatch<SetStateAction>',
    },
  }}
/>

import UseBooleanDemo from '../../../hooks/State/useBoolean/use-boolean.demo';
import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { File, Folder, Files } from 'fumadocs-ui/components/files';
