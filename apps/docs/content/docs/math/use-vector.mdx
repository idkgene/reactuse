---
title: useVector
description: Manage and operate on 3D vectors
---

Demo

## Installation

```bash
npx changeelog/reactuse@latest add useVector
```

## Usage

```tsx
const {
  vector,
  setVector,
  add,
  subtract,
  scale,
  normalize,
  dot,
  cross,
  magnitude,
  isZero,
  unit,
} = useVector({ x: 1, y: 2, z: 3 });

console.log(vector); // { x: 1, y: 2, z: 3 }
add({ x: 1, y: 1, z: 1 });
console.log(vector); // { x: 2, y: 3, z: 4 }
scale(2);
console.log(vector); // { x: 4, y: 6, z: 8 }
console.log(magnitude); // ~10.95
console.log(unit); // { x: ~0.37, y: ~0.55, z: ~0.73 }
```

## Reference

### useVector

`useVector` is a hook that manages a 3D vector and provides various operations on it.

<TypeTable
  type={{
    initialVector: {
      description: 'The initial 3D vector.',
      type: 'Readonly',
      default: '{ x: 0, y: 0, z: 0 }',
    },
  }}
/>

### Return Value

Returns an object with the following properties and methods:

<TypeTable
  type={{
    vector: { description: 'The current vector.', type: 'Readonly' },
    setVector: {
      description: 'Function to set the vector.',
      type: 'React.Dispatch<React.SetStateAction>',
    },
    add: { description: 'Add another vector.', type: 'VectorOperation' },
    subtract: {
      description: 'Subtract another vector.',
      type: 'VectorOperation',
    },
    scale: {
      description: 'Scale the vector by a scalar.',
      type: 'ScalarOperation',
    },
    normalize: { description: 'Normalize the vector.', type: '() => void' },
    dot: {
      description: 'Calculate dot product with another vector.',
      type: '(v: Readonly) => number',
    },
    cross: {
      description: 'Calculate cross product with another vector.',
      type: 'VectorOperation',
    },
    magnitude: { description: 'The magnitude of the vector.', type: 'number' },
    isZero: {
      description: 'Whether the vector is effectively zero.',
      type: 'boolean',
    },
    unit: {
      description: 'The unit vector in the same direction.',
      type: 'Vector3D',
    },
  }}
/>

### Types

```ts
type Numeric = number | { valueOf: () => number };

interface Vector3D {
  readonly x: number;
  readonly y: number;
  readonly z: number;
}

type VectorOperation = (v: Readonly<Vector3D>) => void;
type ScalarOperation = (scalar: Numeric) => void;
```

import { TypeTable } from 'fumadocs-ui/components/type-table';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import { Step, Steps } from 'fumadocs-ui/components/steps';
import { File, Folder, Files } from 'fumadocs-ui/components/files';
