---
title: logicOr
description: Your first document
---

The `logicOr` function (also available as `or` for convenience) performs a logical OR operation on multiple provided values or functions. It efficiently checks if at least one of the provided conditions evaluates to true. The

## Usage

```tsx
import { logicOr } from '#';
import * as React from 'react';

const Component = () => {
  const value1 = true;
  const value2 = false;
  const value3 = () => false;

  const result = logicOr(value1, value2, value3);

  return (
    <div>
      <p>Logical OR result: {result.toString()}</p>
    </div>
  );
};

export default Component;
```

```tsx
import { logicOr } from '#';
import * as React from 'react';

const Component = () => {
  const [value1, setValue1] = React.useState(false);
  const [value2, setValue2] = React.useState(false);

  const result = logicOr(value1, value2, () => true);

  return (
    <div>
      <p>Logical OR result: {result.toString()}</p>
      <button onClick={() => setValue1(true)}>Set value1 to true</button>
      <button onClick={() => setValue2(true)}>Set value2 to true</button>
    </div>
  );
};

export default Component;
```

## Type Declarations

```typescript
export declare function logicOr(...args: Resolvable<any>[]): boolean;

export declare type Resolvable<T> = T | (() => T);
```

## Reference

#### logicOr

| Property | Type                | Default | Description                          |
| :------- | :------------------ | :------ | :----------------------------------- |
| `args`   | `Resolvable<any>[]` | -       | The values or functions to evaluate. |

#### Resolvable

| Property | Type               | Description                                            |
| :------- | :----------------- | ------------------------------------------------------ |
| `T`      | `T   \| (() => T)` | A value or a function that returns a value of type`T`. |

### Returns

| Type      | Description                                                                                                                                                                                  |
| :-------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `boolean` | Returns `true` if any of the evaluated values are true; otherwise, `false`. If any of the provided values or functions are functions, they are called and their return values are evaluated. |

## Common Pitfalls

### Passing Invalid Arguments

Ensure that the arguments passed to `logicOr` are either values or functions that return values. Passing other data types may lead to unexpected behavior.

```tsx
const result = logicOr(true, 'false', null); // Incorrect usage
```

**Solution**: Pass values or functions that return values.

```tsx
const result = logicOr(true, false, () => true);
```

## Alias

The `logicOr` function can also be aliased as `or`.

```tsx
import { or } from '#';
import * as React from 'react';

const Component = () => {
  const value1 = true;
  const value2 = false;
  const value3 = () => false;

  const result = or(value1, value2, value3);

  return (
    <div>
      <p>Logical OR result: {result.toString()}</p>
    </div>
  );
};
```
