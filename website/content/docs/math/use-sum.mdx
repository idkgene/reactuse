---
title: useSum
description: Your first document
---

`useSum` is a custom React hook that takes an array of numbers and calculates the sum of those numbers. It returns the calculated sum as a number.

## Usage

```tsx
import { useSum } from '#';
import * as React from 'react';

const Component = () => {
  const numbers = [1, 2, 3, 4, 5];
  const sum = useSum(numbers);

  return (
    <div>
      <p>Sum: {sum}</p>
    </div>
  );
};

export default Component;
```

```tsx
import { useSum } from '#';
import * as React from 'react';

const Component = () => {
  const [numbers, setNumbers] = React.useState([10, 20, 30]);
  const sum = useSum(numbers);

  const handleAddNumber = () => {
    setNumbers([...numbers, 40]);
  };

  return (
    <div>
      <p>Sum: {sum}</p>
      <button onClick={handleAddNumber}>Add Number</button>
    </div>
  );
};

export default Component;
```

## Type Declarations

```ts
export declare function useSum(array: number[]): number;
```

## Reference

| Property | Type       | Default | Description                                  |
| -------- | ---------- | ------- | -------------------------------------------- |
| `array`  | `number[]` | -       | An array of numbers to calculate the sum of. |

### Returns

| Type     | Description                                |
| -------- | ------------------------------------------ |
| `number` | The sum of the numbers in the input array. |
|          |